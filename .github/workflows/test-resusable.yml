name: CI openDAQ

on:
  workflow_dispatch:
    inputs:
      opendaq-ref:
        description: "openDAQ commit, branch or tag"
        required: false
        default: ""
  push:

jobs:
  ci:
    name: openDAQ
    uses: ./.github/workflows/reusable.yml
    with:
      opendaq-ref: ${{ inputs.opendaq-ref || '' }}
      packages: >
        [
          {
            "match-jobs": ["ubuntu-*"],
            "apt-install": ["lld", "libx11-dev", "libxi-dev", "libxcursor-dev", "libxrandr-dev", "libgl-dev", "libudev-dev", "libfreetype6-dev", "python3-dev"],
            "pip-install": ["numpy"]
          },
          {
            "match-jobs": ["macos-*"],
            "pip-install": ["numpy"]
          },
          {
            "match-jobs": ["windows-*"],
            "pip-install": ["numpy"]
          },
          {
            "match-jobs": ["windows-*-x86-*"],
            "run": "choco install python --x86 -y && refreshenv"
          }
        ]
      timeout: '[{"timeout-minutes": 270}]'
      cmake-presets: >
        [
          {
            "configure-preset": "ci",
            "test-preset": "run_tests"
          },
          {
            "match-jobs": ["windows-*-msvs-*"],
            "configure-preset": "ci-msvc"
          },
          {
            "match-jobs": ["macos-*"],
            "configure-preset": "ci-macos"
          }
        ]

